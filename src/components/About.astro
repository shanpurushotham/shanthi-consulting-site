<section class="section section--alt about">
  <div class="wrap about__layout">
    <div class="section-heading about__intro">
      <span class="eyebrow">About</span>
      <h2>Hey, I'm Shanthi.</h2>
    </div>

    <div class="about__body">
      <p>
        I was standing next to the CEO when we scaled from 5 people to 60, right after
        we closed a $13.5M Series A.
      </p>

      <p>
        I learned where to put the budget and where not to, which marketing bets to
        make first and when to scale them, and how to hire without burning money or
        morale. Along the way I deployed $75M in treasury, opened six new markets
        across West Africa, Asia, and Australia, and built two new revenue lines from
        scratch.
      </p>

      <p>
        The hardest part isn't the growth itself. It's that what your leadership team
        needs at 5 people is completely different from what it needs at 100. Past a
        certain size, your leaders can't keep a personal pulse on everything anymore, so
        your systems have to be real-time, visible to everyone, and understood across
        every team instead. Skip that work and the cost is real — wasted money, lost
        morale, and hires made too fast or in the wrong seats. Your people and the
        culture you build together are the company's real advantage.
      </p>

      <p>
        <strong>Heart and rigor.</strong> Before startups, I did this same kind of work
        in global health: two $20M programs launched across Sub-Saharan Africa, a
        $300M portfolio managed across 15+ countries, systems built inside a refugee
        camp in Bangladesh. When a broken process means someone doesn't get care, you
        learn to take the details seriously. I bring that same standard to your
        company.
      </p>

      <p>
        I've also sat with technical founders at the very beginning — stress-testing a
        prototype and getting ready for their first seed round.
      </p>

      <p class="about__closing">I've done this twice now. I'd like to do it for you next.</p>
    </div>
  </div>
</section>

<style>
  .about__layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
  }

  .about__body {
    max-width: var(--content-narrow);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .about__body p {
    line-height: var(--lh-relaxed);
    color: var(--color-ink-soft);
  }

  .about__body strong {
    color: var(--color-ink);
  }

  .about__closing {
    font-family: var(--font-serif);
    font-size: var(--fs-h3);
    color: var(--color-ink) !important;
  }

  /*
    Single-column, full-width layout until the real photo is ready.
    To bring back the two-column layout with a photo:
    1. Add back a `.about__photo` element (with a `.about__photo-frame` child)
       between `.about__intro` and `.about__body` in the markup.
    2. Restore this media query:

    @media (min-width: 60rem) {
      .about__layout {
        grid-template-columns: 1fr 320px;
        column-gap: var(--space-5);
      }

      .about__intro {
        grid-column: 1;
        grid-row: 1;
      }

      .about__photo {
        grid-column: 2;
        grid-row: 1 / 3;
      }

      .about__body {
        grid-column: 1;
        grid-row: 2;
      }
    }

    .about__photo-frame {
      width: 100%;
      max-width: 320px;
      aspect-ratio: 4 / 5;
      border: 1px dashed var(--color-border);
      border-radius: var(--radius);
      background: var(--color-bg);
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: var(--color-ink-soft);
      font-size: var(--fs-small);
      line-height: var(--lh-normal);
    }
  */
</style>
